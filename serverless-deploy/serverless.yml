service: serverless-test

frameworkVersion: '2'

provider:
  name: aws
  runtime: python3.8
  region: ap-southeast-2
  # profile: admin


functions:


resources:
 Resources:
   NewResource:
     Type: AWS::S3::Bucket
     Properties:
       BucketName: my-new-bucket-qifan

